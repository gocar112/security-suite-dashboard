# Runtime state - findings and triage are local to each deployment
data/
config.json
.env

# Vulnerability feed cache: keep the directory, never commit the payload
nvds/*
!nvds/.gitkeep

# Quarantined files are malicious by definition - never commit them
quarantine/*
!quarantine/.gitkeep

# Watched folder: keep the directory, never commit what lands in it
uploads/*
!uploads/.gitkeep

# Safer home-user drop zone: only suspicious files should be copied here
SecurityDrop/*
!SecurityDrop/.gitkeep

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
